# Personal Profile Vault

Create with `/profile create`, add values with `/profile set field :: value`, and register documents with `/profile add-file label :: path`. `/profile show` and export previews mask sensitive fields. Data is local under ignored `data/profile/`; never copy that folder into source control.
