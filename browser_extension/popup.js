const out=document.querySelector("#result");chrome.storage.local.get(["port","token"],x=>{port.value=x.port||8765;token.value=x.token||""});save.onclick=()=>chrome.storage.local.set({port:Number(port.value),token:token.value},()=>out.textContent="Saved locally.");document.querySelectorAll("[data-action]").forEach(b=>b.onclick=async()=>{let [tab]=await chrome.tabs.query({active:true,currentWindow:true});let action=b.dataset.action;chrome.tabs.sendMessage(tab.id,{action:action==="sensitive"?"fill":action,sensitive:action==="sensitive"},r=>out.textContent=JSON.stringify(r||{error:chrome.runtime.lastError?.message},null,2))});
